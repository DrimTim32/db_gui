<Page x:Class="Project.BarApplication.Pages.Menagement.Categories"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Project.BarApplication.Pages.Menagement"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="Categories">

    <Grid>
        <DataGrid x:Name="CategoriesGrid" AutoGenerateColumns="False" ItemsSource="{Binding Categories}">
            <DataGrid.Columns >
                <DataGridTextColumn Header="Slug" Binding="{Binding Path=Slug}"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Path=Name}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
